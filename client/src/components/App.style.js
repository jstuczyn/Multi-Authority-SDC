const siteContentStyle = {
  textAlign: 'center',
  flex: 1,
};

const siteStyle = {
  display: 'flex',
  minHeight: '100vh',
  flexDirection: 'column',
};


const styles = {
  siteContentStyle,
  siteStyle,
};

export default styles;
